# TITANOS // LAUNCH SEQUENCE 001 — "THE OBELISK AWAKENS"

Added 2026-08-25 per Kyle's explicit instruction. Loaded at session start
via this project's `CLAUDE.md` — stateless config, not session memory.
Sixteenth doctrine file. Compressed per the same discipline as
`TITANOS_GREENLIGHT_AND_MEMETIC_DOCTRINE.md` — this directive is
~90%+ restatement of the fifteen files already loaded (GO Cycle's
four-agent/ten-phase shape, `recursion_guard.py`'s ancestry contract,
the invariant-transfer discipline already exercised via
`SIGIL_LEXICON.md`, `foundation/crystal.py`'s problem/hypothesis/
evidence/result compression, Critical Function Switch-Gate's fail-closed
human-authority boundary). Only the genuinely new piece is recorded as
new doctrine; everything else is a pointer to the existing implementation.

## WHAT WAS ALREADY FULLY COVERED (not restated)

Four-agent cross-examination, CT_141, the ten-phase GO cycle, recursion
ancestry/bounded-descent (`foundation/recursion_guard.py`), human
authority gates, fail-closed-on-unknown, "never leave a problem as a
dead end," capability-only language, proof-requires-evidence-not-
assertion. All already governed by `TITANOS_GO_CYCLE_DOCTRINE.md` and
enforced in the code it references.

## WHAT WAS GENUINELY NEW

### The `GO <topic>` universal digestion interface

Prior doctrine's `GO` (bare word) means "self-navigate the next internal
capability cycle inside this repo." This directive generalizes the verb:
`GO <topic>` means "digest this external domain/document/knowledge
through the existing pipeline and produce exactly one durable structural
delta, or none." This is a genuinely new *interface contract*, not a
new *mechanism* — the mechanism it invokes already exists, audited
same day:

| Digestion Law step | Real implementation |
|---|---|
| RAW INPUT → provenance boundary | `kpm/source-vault/registry.py::SourceRegistry.ingest_source()` — content-addressed, hashed, archived, append-only ledger |
| Normalise → extract claims | `narrative/intake/source_vault_bridge.py::source_record_to_narrative_atom_yaml()` |
| Atomise / classify | `narrative/store/narrative_atom_store.py` + `kpm/schemas/epistemic_types.py` (15-value classification, forbidden transitions, frozen history) |
| Check duplication/contradiction | `narrative/composition/checker.py`, `kpm/contradictions/registry.py` |
| Extract candidate invariant | `SIGIL_LEXICON.md`'s existing transfer-specimen discipline (4 registered this session) |
| Durable delta / compaction | `foundation/crystal.py::Crystal`/`CrystalStore` |

**Net finding: the digestion organ this directive asks for already
exists end-to-end.** What has never happened is a real, non-synthetic
external artifact flowing through it — exactly the FIRST_PING_REFRAME_001
finding from earlier this session (candidate 1: Kyle supplies one real
external artifact → `ingest_source()` → classify → `Crystal` → return
result). This directive and that finding converge on the same next
action from two different directions — recorded as corroborating
evidence, not two separate frontier items.

### Explicit epistemic-marker vocabulary in output (◈ ↳ ? ⌁ ⚖ → ✓ ⊘)

A finer-grained *display* convention than `epistemic_types.py`'s 15-value
enum, for cycle reports specifically. Adopted as a reporting convention
for future `GO <topic>` cycles; not a new classification system — maps
onto the existing enum, does not replace it.

## WHAT WAS NOT BUILT THIS CYCLE

No code. This directive supplied a doctrine and an interface contract,
not a topic — `GO <topic>` requires an actual `<topic>` to execute
against, per this file's own Autonomy Law ("if the system cannot
determine the next move safely: recon → report the blocker → stop").
Inventing a synthetic topic to demonstrate the loop would violate the
Reality Law this same directive states ("the system must not evolve
purely through internal agreement") — the loop must be proven on a real
external artifact, not a fabricated one. Per this session's own
Predecessor Law (§ "close the chain before claiming the next cycle"):
the standing predecessor item is FIRST_PING_REFRAME_001's candidate 1,
unresolved, waiting on Kyle to supply real external content.

## STANDING NEXT ACTION

`GO <topic>` is now live as the standing interface. The first real
invocation should be Kyle supplying one genuine external artifact
(a document, a claim, a URL's pasted text — not this session inventing
one) for the existing digestion pipeline to actually process end-to-end
for the first time.
