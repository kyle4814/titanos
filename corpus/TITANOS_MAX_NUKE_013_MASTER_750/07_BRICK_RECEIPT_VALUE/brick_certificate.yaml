schema: titanos_brick_certificate
version: "1.0"
domain: "07_BRICK_RECEIPT_VALUE"
status: draft
contract:
  scope: bounded
  inputs: explicit
  outputs: explicit
  dependencies: versioned
  evidence: required
  receipt: required
guards:
  - authority
  - provenance
  - validation
  - acceptance
