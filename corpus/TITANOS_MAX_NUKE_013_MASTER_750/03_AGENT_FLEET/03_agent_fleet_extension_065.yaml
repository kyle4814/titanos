schema: titanos_03_agent_fleet_extension_065
version: "1.0"
domain: "03_AGENT_FLEET"
status: draft
contract:
  scope: bounded
  inputs: explicit
  outputs: explicit
  dependencies: versioned
  evidence: required
  receipt: required
guards:
  - authority
  - provenance
  - validation
  - acceptance
