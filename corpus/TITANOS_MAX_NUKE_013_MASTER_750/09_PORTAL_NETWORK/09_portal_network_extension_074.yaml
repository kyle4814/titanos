schema: titanos_09_portal_network_extension_074
version: "1.0"
domain: "09_PORTAL_NETWORK"
status: draft
contract:
  scope: bounded
  inputs: explicit
  outputs: explicit
  dependencies: versioned
  evidence: required
  receipt: required
guards:
  - authority
  - provenance
  - validation
  - acceptance
