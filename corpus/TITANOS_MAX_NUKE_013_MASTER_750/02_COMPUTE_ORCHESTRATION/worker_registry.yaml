schema: titanos_worker_registry
version: "1.0"
domain: "02_COMPUTE_ORCHESTRATION"
status: draft
contract:
  scope: bounded
  inputs: explicit
  outputs: explicit
  dependencies: versioned
  evidence: required
  receipt: required
guards:
  - authority
  - provenance
  - validation
  - acceptance
