# SUPPORT CONTRACT 0273 — CLASSIFICATION_RULE

This artifact is a machine-readable engineering doctrine companion.

Required:
- inspect existing capability;
- preserve provenance;
- avoid authority confusion;
- keep worker context disposable;
- persist durable state;
- receipt material mutations;
- checkpoint risky mutations;
- test behaviour;
- adversarially verify assumptions;
- distinguish declared, implemented, verified and realized states.

This document is a contract, not proof that the capability already exists.
