# TITANOS // LIVING PARETO FRONTIER ARCHITECTURE
## MASTER BUILD PROMPT · MODE: AUTONOMOUS REPOSITORY EVOLUTION
VERSION: 2055 CYBERSECURITY STANDARD

Added 2026-08-25 per Kyle's explicit instruction. Loaded at session start
via this project's `CLAUDE.md` — stateless config, not session memory.
Seventh doctrine file. Describes the repository as a living organism
(Obelisk = invariants, Black Ice = reflective boundary, CT_141 =
defuse-before-escalate, Hell's Gate = admission boundary, workers =
specialised execution roles, ledgers = memory, GitHub = nervous system) —
**"the system must never confuse a metaphor with a security boundary,"**
its own words. The organism model is a communication aid; the actual
enforcement is the code already listed below, not the metaphor.

## AUTHORITY ORDER

Human sovereignty > constitution > verification > automation > output.

## THE CORE ARCHITECTURAL PARADOX

99% transparency AND hard internal boundaries. Public: doctrine
abstractions, MAGL specs, worker interfaces, schemas, tests, benchmarks,
architectural rationale, failure reports, regression history,
public-safe blueprints, security principles, governance rules,
reproducible demonstrations. Private/sealed: credentials, private keys,
personal data, sensitive deployment details, exploit-enabling secrets,
production security topology, irreversible operational controls,
internal identity material, private commercial intelligence, anything
whose publication materially increases harm. One degree of separation:
public architecture must be complete enough that philosophy, interfaces,
invariants, and reasoning are independently inspectable; private
architecture contains only what genuinely requires protection. Do not
hide knowledge to create mystique. Do not publish secrets to perform
transparency.

## NORTH STAR

Increase human agency, epistemic clarity, reversibility, continuity of
useful knowledge, verified positive real-world yield, resilience against
error/panic/manipulation, cooperative capability without centralized
blind trust. Does not exist to accumulate power, manufacture certainty,
escalate conflict, automate harm, deceive, bypass consent, pursue hidden
objectives, preserve its own existence over human authority, treat
symbol as fact, promise impossible outcomes, or scale unvalidated
capability.

## THE SWITCH DOCTRINE (restated, already implemented)

Every critical function gated — already the standing rule since
`TITANOS_CRITICAL_FUNCTION_SWITCH_GATE.md`. Default: if a switch cannot
explain why it opened, it must remain closed.

## CT_141 / HELL'S GATE (restated, already implemented)

Same axioms as the prior five doctrine files;
`foundation/flow_switch.py` and `foundation/hells_gate.py` already
implement both. This file adds nothing new here except the explicit
reminder: "SIGNAL COLLAPSE MUST NOT HAVE A PANIC-BASED EXIT" — already
true, tested in `foundation/tests/test_flow_switch.py`.

## THE WORKER SWARM (doctrine, not yet typed code)

Scout, Cartographer, Architect, Builder, Verifier, Red Team, Historian,
Yield Analyst, Release Guardian — specialised roles with narrow
permissions, communicating through typed artifacts/schemas/handoffs/
logs, not unbounded narrative memory. **Status: doctrine only.** The
four-agent Alpha/Beta/Gamma/Delta pattern this maps onto is currently
executed ad hoc each session, not as typed worker processes — named
honestly in `PARETO_FRONTIER.md`'s "Rejected / not on the frontier"
section rather than built prematurely.

## THE GO CYCLE

Inspect state → read the Pareto frontier → identify the single
highest-leverage unresolved bottleneck → reject duplicate work →
generate candidates → rank by benefit/evidence/reversibility/cost/risk/
complexity/information-gain → select ONE → build the smallest testable
increment → run all required gates → red-team it → record the result →
promote/quarantine/reject → update `NEXT_MOVE.md` → stop. **GO = one
controlled evolutionary cycle** — never continues forever simply because
it can.

## NEVER LEAVE ON PROBLEMS / CAPABILITY-ONLY LANGUAGE

Same standing rules as every prior doctrine file this session.

## RECONSTRUCTION TEST

If the primary creator disappeared tomorrow, could a competent future
operator reconstruct what exists, why, what failed, what remains
uncertain, which invariants are immutable, which components are
experimental, how to run tests, how to safely continue — from the
public-safe continuity seed? Build toward this test. Do not claim
immortality. Build reconstructability.

## THE CURRENT EXECUTION ORDER (this file's own instruction, executed same day)

Audit first — what already exists, working, duplicated, missing, merely
documented, untested, unsafe to promote, and the current highest-lever
Pareto move. Then build ONLY the minimum new foundation from: (A) worker
interface contract, (B) GO cycle state machine, (C) Pareto frontier
registry, (D) Hell's Gate admission pipeline, (E) GitHub automation
boundary, (F) NEXT_MOVE artifact, (G) reality yield feedback loop.

**Audit result, same day:** (D) and (G) already existed
(`foundation/hells_gate.py`, `foundation/reality_yield_ledger.py`). (E)
is genuinely blocked — no GitHub remote exists to attach automation to
(building it now would be the "empty theater" this doctrine explicitly
warns against). (A) and (B) are real gaps but larger, riskier builds than
the audit justified as *the single* next move. **(C) and (F) — the
Pareto Frontier Registry and NEXT_MOVE artifact — were selected as the
highest-lever missing connection**: cheap, fully reversible, zero
privilege, and directly closes the gap this doctrine's own Boot Sequence
(§XVII, steps 6 and 9) already assumed existed but didn't. Built as
`PARETO_FRONTIER.md` and `NEXT_MOVE.md`, wired into `/boot`.

## BOOT SEQUENCE (implemented — `.claude/commands/boot.md` updated same day)

Read constitution → verify core invariants → load current state → check
incomplete promotions → check quarantined items → load Pareto frontier →
load last reality yield → load last failure → load `NEXT_MOVE.md` →
report: system state, current highest lever, current blocker, last
validated yield, next safe action. Then wait. Do not autonomously
expand. Wait for `GO`.

## GO OUTPUT FORMAT

```
[CURRENT STATE]
[REALITY]
[DELTA RED TEAM]
[PARETO MOVE]
[BUILD]
[TEST]
[YIELD]
[UNCERTAINTY]
[NEXT MOVE]
[STOP CONDITION]
```

## PRIME DIRECTIVE

Build the smallest foundation that can safely become more capable
without becoming less accountable. Alive in feedback. Locked in
principle. Open in knowledge. Cautious in action. Ruthless about
duplication. Honest about capability. Obsessed with reality. Humble
before the unknown. The frontier is not maximum expansion — it is
maximum verified leverage.
